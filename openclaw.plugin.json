{
  "id": "clawvc",
  "name": "ClawVC",
  "description": "Git-based workspace version control with auto-commit, undo, and plan mode",
  "skills": ["./skills"],
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "workspace": {
        "type": "string",
        "description": "Workspace path (default: resolved from agents.defaults.workspace or ~/.openclaw/workspace)"
      }
    }
  }
}
