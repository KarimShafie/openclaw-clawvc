{
  "id": "clawvc",
  "name": "ClawVC",
  "description": "Git-based workspace version control with auto-commit, undo, and plan mode",
  "skills": ["./skills"],
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "workspace": {
        "type": "string",
        "description": "Workspace path to watch (default: resolved from agents.defaults.workspace or ~/.openclaw/workspace)"
      },
      "autoWatch": {
        "type": "boolean",
        "description": "Auto-start file watcher on gateway startup",
        "default": true
      },
      "debounceMs": {
        "type": "integer",
        "description": "Milliseconds to wait after last file change before committing",
        "default": 3000,
        "minimum": 500,
        "maximum": 30000
      }
    }
  }
}
